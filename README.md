This repo contains the solution to the challenge of Light-It

## Download and Installation

1- Clone the repo with `git clone https://github.com/Marcos-Iorio/light-it-challenge.git`
2- Run `pnpm install` to install all the packages required
3- Go to localhost:3000 in your browser to see the app working

## Tech stack

- Next.js
- Typescript
- Tailwind
- Zod(for form validations)
- React Hook Form(for managing the forms)
- Zustand(for managing global states)
- Eslint(for code linting)
- Vitest, React Testing library(to test components, stores, schemas)

## Codebase

The entry point is the `page.tsx` a SSR component that fetches all the data and hydrate the `PatientStore` with the data to be available in the whole app.
I put the reusable componentes in unique files and all the related Patient components below a **Patient** folder
The mayority of the types are under `types.ts`.
/utils have useful and reusable utilitarie functions
`__test__` has all the test code generated by Claude Code using the context of all the components.

I want the design to be minimalist and sober, simple, so I choose a creamy color in combination with a English Green for the accents, smooth animation for entry and out of the Modal and Toaster.

I used Zustand to manage stores of Patient, Toaster and Modal to enable globally without generating a provider and making a provider hell in the main file. Zustand allows me to create an Store with logic and called where it's necessary.

Zod + React Hook Form help to create a nice form validation and managing all the data and sumbmitting function.

Toaster, Button and Modal meant to be reusable, fully typed components

All the styles of the app where made by me without any UI libraries.

I coded the 85% of the codebase by myself, Claude Code in plan mode help me to choose technical decisions and made the test for all the code. All the test passed succesfully.

### TODO

- Make a search bar to facilitate the search of a Patient
